{% extends "baseAdmin.html.twig" %}



{% block body %}


  <div class="main-panel">        
        <div class="content-wrapper">
          <div class="row">
            <div class="col-12 grid-margin stretch-card">
              <div class="card">
                <div class="card-body">
                  <h4 class="card-title">Edit a tresorie</h4>
                  {# <p class="card-description">
                    Basic form elements
                  </p> #}
                  
                    
                    {{ form_start(editForm) }}
                    {{ form_row(editForm.solde_r, {'attr': {'class': 'form-control'}}) }}
                    {{ form_row(editForm.entree, {'attr': {'class': 'form-control'}}) }}
                    {{ form_row(editForm.descE, {'attr': {'class': 'form-control'}}) }}
                    {{ form_row(editForm.sortie, {'attr': {'class': 'form-control'}}) }}
                    {{ form_row(editForm.descS, {'attr': {'class': 'form-control'}}) }}
                    {# {{ form_row(editForm.banque, {'attr': {'class': 'form-control'}}) }} #}
                    {{ form_row(editForm.Add, {'label': 'Modifier', 'attr': {'class': 'btn btn-success'}}) }}
                    {{ form_end(editForm) }}

                </div>
              </div>
            </div>
          </div>
        </div>
        <!-- content-wrapper ends -->
      </div






{% endblock %}